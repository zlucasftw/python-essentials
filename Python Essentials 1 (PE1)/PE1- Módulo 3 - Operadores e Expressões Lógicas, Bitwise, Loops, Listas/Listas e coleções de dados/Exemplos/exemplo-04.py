# Listas - coleções de dados: Indexação

numbers = [10, 5, 7, 2, 1]

numbers[0] = 111

numbers[1] = numbers[4]  # Copying value of the fifth element to the second.

print(numbers[0])  # Acessing the list's first element.

print(numbers)  # Printing the whole list.
