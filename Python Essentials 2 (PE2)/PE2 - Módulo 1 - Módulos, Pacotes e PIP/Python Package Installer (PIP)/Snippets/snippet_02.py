# Instalação do pip em Linux

# Em alguns tipos de distribuições Linux podem já ter o pip
# pré-instalado ou até mesmo utilizar mais de uma versão
# do Python simultaneamente. Nesse caso pode-se
# ter o Python 2 como padrão e precisa-se especificar explicitamente
# o uso do Python 3, podendo haver dois pips diferentes
# o pip (ou pip2) e pip3

# Comando que verifica a versão do pip e também a qual versão do Python ele está associado

# pip --version

# pip3 --version
