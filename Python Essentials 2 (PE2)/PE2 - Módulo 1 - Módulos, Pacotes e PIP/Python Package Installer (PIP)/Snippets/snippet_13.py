# Como utilizar o pip

# Decidindo-se qual pacote
# vai se instalar no computador vai se existir
# dois cenários possíveis que é instalar o pacote
# apenas a nível de usuário ou, se tem direitos de administrador
# instalar o pacote em todo o sistema caso desejado

# Para distinguir entre as duas ações o pip utiliza
# uma opção dedicada denominada '--user' o que instrui
# o pip a agir localmente em nome do utilizador, caso não seja utilizado
# ele assume que é um administrador do sistema.
