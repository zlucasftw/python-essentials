# Métodos de String

# Método strip()

# O método strip() combina os efeitos causados pelos métodos
# rstrip() e lstrip(), criando uma nova string sem espaços
# em branco tanto à esquerda quanto à direita.

# Demonstrating the strip() method:
print("[" + "   aleph   ".strip() + "]")
