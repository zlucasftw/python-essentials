# Métodos de String

# Método find()

# Se o objetivo é apenas verificar se um único
# caractere está em uma string é importante notar
# que o operador in será significamente mais rápido

t = 'theta'
print(t.find('eta'))
print(t.find('et'))
print(t.find('the'))
print(t.find('ha'))
