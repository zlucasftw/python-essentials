# Métodos de String

# Método replace()

# O método replace() tem uma variante de três parâmetros
# onde o terceiro argumento, sendo um número, que serve
# para limitar o número de substituições

print("This is it!".replace("is", "are", 1))
print("This is it!".replace("is", "are", 2))
