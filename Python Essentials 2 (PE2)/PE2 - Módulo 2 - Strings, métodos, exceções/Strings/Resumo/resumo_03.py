# Resumo da secção - Key takeaways

# Strings podem ser concatenadas usando o operador '+'
# e replicadas usando o operador '*'

asterisk = '*'
plus = "+"
decoration = (asterisk + plus) * 4 + asterisk
print(decoration)
